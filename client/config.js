// Change this to your deployed API URL when you deploy (see the deployment guide).
// Local development default assumes the server from /server is running on port 4000.
window.API_BASE = 'http://localhost:4000/api';
